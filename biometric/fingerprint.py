import subprocess

def authenticate_with_fingerprint():
    # result = subprocess.run(['FingerprintHelper.exe'], capture_output=True, text=True)
    # return result.stdout.strip() == "Success"
    print("Simulating fingerprint scan...")
    return True  # Pretend fingerprint was successful